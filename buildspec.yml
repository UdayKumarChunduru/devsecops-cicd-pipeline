version: 0.2

phases:
  install:
    commands:
      - TRIVY_VERSION=0.58.1
      - curl -sSLo trivy.tar.gz "https://github.com/aquasecurity/trivy/releases/download/v${TRIVY_VERSION}/trivy_${TRIVY_VERSION}_Linux-64bit.tar.gz"
      - curl -sSLo trivy_checksums.txt "https://github.com/aquasecurity/trivy/releases/download/v${TRIVY_VERSION}/trivy_${TRIVY_VERSION}_checksums.txt"
      - grep "trivy_${TRIVY_VERSION}_Linux-64bit.tar.gz" trivy_checksums.txt | sha256sum -c -
      - tar -xzf trivy.tar.gz trivy
      - mv trivy /usr/local/bin/trivy
      - chmod +x /usr/local/bin/trivy
  pre_build:
    commands:
      - echo "codebuild resolved source version is $CODEBUILD_RESOLVED_SOURCE_VERSION"
      - aws ecr get-login-password --region $AWS_REGION | docker login --username AWS --password-stdin $ECR_REPOSITORY_URL
      - IMAGE_TAG=$(echo $CODEBUILD_RESOLVED_SOURCE_VERSION | cut -c1-8)
      - echo "resolved image tag is $IMAGE_TAG"
  build:
    commands:
      - docker build -t $ECR_REPOSITORY_URL:$IMAGE_TAG app/
  post_build:
    commands:
      - docker push $ECR_REPOSITORY_URL:$IMAGE_TAG
      - trivy image --exit-code 1 --severity CRITICAL --ignorefile .trivyignore --no-progress $ECR_REPOSITORY_URL:$IMAGE_TAG
      - trivy image --format cyclonedx --output sbom-$IMAGE_TAG.json $ECR_REPOSITORY_URL:$IMAGE_TAG
