---
- name: Tear down the terraform-aws-cloud branch stack
  hosts: local
  gather_facts: false
  vars_files:
    - ../group_vars/all/vars.yml

  tasks:
    - name: Check if a cluster is reachable
      ansible.builtin.command:
        cmd: kubectl get nodes
      register: cluster_check
      failed_when: false
      changed_when: false

    - name: Uninstall falco
      kubernetes.core.helm:
        name: falco
        release_namespace: falco
        state: absent
      when: cluster_check.rc == 0
      failed_when: false

    - name: Uninstall kyverno
      kubernetes.core.helm:
        name: kyverno
        release_namespace: kyverno
        state: absent
      when: cluster_check.rc == 0
      failed_when: false

    - name: Force delete remediation lambda
      amazon.aws.lambda:
        name: "falco-remediation"
        state: absent
        region: "{{ aws_region }}"
      failed_when: false

    - name: Destroy terraform managed infra
      ansible.builtin.command:
        cmd: terraform destroy -auto-approve -input=false
        chdir: "{{ terraform_dir }}"
      register: infra_destroy
      changed_when: true
      failed_when: false
      no_log: true

    - name: Scan for any lingering resources tagged for this project
      ansible.builtin.command: >
        aws resourcegroupstaggingapi get-resources
        --region us-east-1
        --tag-filters Key=project,Values=devsecops-pipeline
        --query "ResourceTagMappingList[*].ResourceARN"
        --output json
      register: active_resources
      changed_when: false
      failed_when: false

    - name: Report any lingering project resources
      ansible.builtin.debug:
        msg: "{{ active_resources.stdout | from_json }}"
      when:
        - active_resources.stdout is defined
        - (active_resources.stdout | from_json | length) > 0

    - name: Confirm clean teardown when nothing lingers
      ansible.builtin.debug:
        msg: "no project tagged resources remain in us-east-1"
      when:
        - active_resources.stdout is defined
        - (active_resources.stdout | from_json | length) == 0

    - name: Remove terraform metadata recursively
      ansible.builtin.shell: |
        find "{{ project_root }}/terraform/environments/aws-cloud" -type d -name ".terraform" -prune -exec rm -rf {} +
        find "{{ project_root }}/terraform/environments/aws-cloud" -name ".terraform.lock.hcl" -delete
        rm -f "{{ project_root }}/.terraform-outputs.json"
      when: infra_destroy.rc == 0
      changed_when: true
      failed_when: false

    - name: Teardown complete
      ansible.builtin.debug:
        msg: |
          Teardown complete. Confirm nothing billable remains:
          - aws eks list-clusters
          - aws ecr describe-repositories
          - aws lambda list-functions
          - aws ec2 describe-instances --filters "Name=tag:project,Values=devsecops-pipeline"
          - aws efs describe-file-systems
          note: terraform/bootstrap (state bucket, lock table, oidc role) is intentionally not destroyed here, it is meant to persist across every apply/destroy cycle of the real infrastructure
