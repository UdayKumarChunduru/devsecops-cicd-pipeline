---
- name: Tear down the terraform-aws-cloud branch stack completely
  hosts: local
  gather_facts: false
  vars_files:
    - ../group_vars/all/vars.yml

  tasks:
    - name: Check if a cluster is reachable
      ansible.builtin.command:
        cmd: kubectl get nodes
      register: cluster_check
      failed_when: false
      changed_when: false

    - name: Uninstall falco
      kubernetes.core.helm:
        name: falco
        release_namespace: falco
        state: absent
      when: cluster_check.rc == 0
      failed_when: false

    - name: Uninstall kyverno
      kubernetes.core.helm:
        name: kyverno
        release_namespace: kyverno
        state: absent
      when: cluster_check.rc == 0
      failed_when: false

    - name: Force delete remediation lambda
      amazon.aws.lambda:
        name: "falco-remediation"
        state: absent
        region: "{{ aws_region }}"
      failed_when: false

    - name: Pre-emptively purge secrets to bypass AWS retention windows
      ansible.builtin.shell: |
        for secret in jenkins-admin-user jenkins-admin-password sonar-admin-password snyk-token sonar-token; do
          aws secretsmanager delete-secret --secret-id "devsecops-pipeline/$secret" --force-delete-without-recovery --region us-east-1 || true
        done
      changed_when: true

    - name: Destroy the real environment
      ansible.builtin.command:
        cmd: terraform destroy -auto-approve -input=false
        chdir: "{{ terraform_dir }}"
      register: infra_destroy
      changed_when: true
      failed_when: false

    - name: Destroy the state backend bootstrap
      ansible.builtin.command:
        cmd: terraform destroy -auto-approve -input=false
        chdir: "{{ project_root }}/terraform/bootstrap"
      register: bootstrap_destroy
      changed_when: true
      failed_when: false

    - name: Scan for any lingering resources tagged for this project
      ansible.builtin.command: >
        aws resourcegroupstaggingapi get-resources
        --region us-east-1
        --tag-filters Key=project,Values=devsecops-pipeline
        --query "ResourceTagMappingList[*].ResourceARN"
        --output json
      register: active_resources
      changed_when: false
      failed_when: false

    - name: Report any lingering project resources
      ansible.builtin.debug:
        msg: "{{ active_resources.stdout | from_json }}"
      when:
        - active_resources.stdout is defined
        - (active_resources.stdout | from_json | length) > 0

    - name: Confirm clean teardown when nothing lingers
      ansible.builtin.debug:
        msg: "no project tagged resources remain in us-east-1"
      when:
        - active_resources.stdout is defined
        - (active_resources.stdout | from_json | length) == 0

    - name: Remove terraform metadata recursively
      ansible.builtin.shell: |
        find "{{ project_root }}/terraform" -type d -name ".terraform" -prune -exec rm -rf {} +
        find "{{ project_root }}/terraform" -name ".terraform.lock.hcl" -delete
        find "{{ project_root }}/terraform" -name "terraform.tfstate" -delete
        find "{{ project_root }}/terraform" -name "terraform.tfstate.backup" -delete
        rm -f "{{ project_root }}/.terraform-outputs.json"
      when:
        - infra_destroy.rc == 0
        - bootstrap_destroy.rc == 0
      changed_when: true
      failed_when: false

    - name: Remove local build artifacts and tooling caches
      ansible.builtin.file:
        path: "{{ item }}"
        state: absent
      loop:
        - "{{ lambda_dir }}/lambda-package"
        - "{{ lambda_dir }}/falco_remediation.zip"

    - name: Find and remove pycache and pytest cache directories
      ansible.builtin.shell: |
        find "{{ project_root }}" -type d -name "__pycache__" -exec rm -rf {} + 2>/dev/null
        find "{{ project_root }}" -type d -name ".pytest_cache" -exec rm -rf {} + 2>/dev/null
        find "{{ project_root }}" -name "sbom-*.json" -delete 2>/dev/null
        find "{{ project_root }}" -name "gitleaks-report.json" -delete 2>/dev/null
      changed_when: false

    - name: Teardown complete
      ansible.builtin.debug:
        msg: |
          Teardown complete, both the real environment and the state backend bootstrap were destroyed.
          Confirm nothing billable remains:
          - aws eks list-clusters
          - aws ecr describe-repositories
          - aws lambda list-functions
          - aws ec2 describe-instances --filters "Name=tag:project,Values=devsecops-pipeline"
          - aws efs describe-file-systems
          - aws s3 ls | grep devsecops-pipeline
          Note: the github secret AWS_GITHUB_ACTIONS_ROLE_ARN is now stale since bootstrap was destroyed,
          the next make infra recreates the role with the same name but a new arn, update the secret before the next deploy.
