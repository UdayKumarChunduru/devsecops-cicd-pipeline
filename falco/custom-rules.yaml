customRules:
  rules: |-
    - rule: Privilege Escalation In Demo Container
      desc: Detect setuid binaries or sudo execution inside demo-service pods
      condition: >
        spawned_process and container
        and k8s.pod.label.app = "demo-service"
        and (proc.name in (sudo, su) or proc.args contains "chmod +s")
      output: >
        Privilege escalation attempt in demo pod
        (user=%user.name command=%proc.cmdline pod=%k8s.pod.name ns=%k8s.ns.name)
      priority: CRITICAL
      tags: [container, privilege_escalation]

    - rule: Sensitive File Read In Demo Container
      desc: Detect reads of credential files inside demo-service pods
      condition: >
        open_read and container
        and k8s.pod.label.app = "demo-service"
        and fd.name in (/etc/shadow, /etc/sudoers, /root/.ssh/id_rsa,
                        /var/run/secrets/kubernetes.io/serviceaccount/token)
        and not proc.name in (java)
      output: >
        Sensitive file opened for reading in demo pod
        (file=%fd.name command=%proc.cmdline pod=%k8s.pod.name ns=%k8s.ns.name)
      priority: CRITICAL
      tags: [container, filesystem]
